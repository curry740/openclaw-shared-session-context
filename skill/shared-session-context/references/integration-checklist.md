# Integration Checklist

- OpenClaw is installed and running
- OpenViking is reachable
- `OV_CONTEXT_ROOT` is set
- `OV_IDENTITY_MAP` exists
- target channels are known
- canonical user mapping is defined
- before-answer hook calls context read
- after-answer hook calls structured writeback
- smoke test covers one real continuation flow
